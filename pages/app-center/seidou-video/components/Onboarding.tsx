import { Dispatch, SetStateAction } from "react";

type Props = {
 setUserInterests: Dispatch<SetStateAction<string[]>>
}
const Onboarding = ({setUserInterests}: Props) => {
    return ( 
        <div className="bg-black">
            
        </div>
    );
}

export default Onboarding